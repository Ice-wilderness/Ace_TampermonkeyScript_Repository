# AGENTS.md

## 仓库与范围

本仓库是自用油猴 / 用户脚本合集。每个 `.js` 脚本独立维护，`README.md` 和 `CHANGELOG.md` 按脚本分区记录。

- 默认使用简体中文回复，并编写面向用户的文档。
- 围绕当前需求确定目标脚本和修改范围。只读取完成任务所需的代码与文档；读取其他脚本作为参考不代表它们进入修改范围。
- 保持改动聚焦，沿用目标脚本的现有风格，不顺手增加功能、重构或格式化无关代码。保留用户已有的未提交改动。
- 跨脚本维护时，先说明涉及哪些脚本，再分别实施；各脚本独立决定版本和更新记录。

## 执行与完成标准

在用户已授权的范围内，持续完成实现、必要验证和发现问题的修复。常规实现选择、可逆的本地编辑和相关检查可直接进行，无需逐步请求确认。用户要求分析、提案或等待确认时，遵守该边界。

只有缺失信息会实质影响需求、数据兼容性或授权范围，且无法从上下文判断时，才提出具体问题；同时推进不依赖答案的工作。已获授权的操作不重复确认，提交和发布按用户明确要求执行。

技能和参考资料按任务需要使用，不因关键词相近就加载无关流程。在系统与开发者约束内，用户的明确要求优先于本文件和技能中的默认建议。若某条规则确实导致暂停，指出具体文件和原文，并解释阻塞原因。

完成意味着请求范围内的工作已落实、相关检查已完成，剩余限制已如实说明。验证强度应匹配改动：文档或文案修改检查差异即可；逻辑修改验证受影响行为；无需为小改动新增测试框架。检查通过后，仅在新改动、失败或未解决疑点需要时扩大或重复验证。无法进行目标站点实测时，明确区分静态检查和实际运行结果。

## 用户脚本约束

- 保持 GM 存储 key 和已有数据可用。数据结构或导入导出格式改变时，兼容旧格式或提供迁移；保留备份与恢复能力，处理异常输入。
- 依赖、权限和适配范围以任务所需为限。调整 GM API、网络请求或适配页面时，同步核对相关 `@grant`、`@connect`、`@require`、`@match` / `@include`，使开发中的脚本也能正确运行。
- SPA、懒加载和滚动加载场景应避免重复事件绑定、重复插入元素，以及失控的 MutationObserver、定时器或缓存。
- UI 优先使用稳定选择器，必要时提供降级逻辑；网络请求考虑跨域、频率限制和错误处理。

## 版本与文档的更新时机

普通开发、修复、调试和重构不自动更新 `@version`、`README.md` 或 `CHANGELOG.md`。仅修改文档、AGENTS 或注释不递增脚本版本。

- 用户明确要求“收尾”“整理最终版本”“准备发布”“可以提交了”或创建提交、发布时，按下节统一检查受影响脚本的版本与文档。
- 用户仅要求更新某份文档或版本时，按指定范围执行，不自动扩展成全面收尾。更新版本记录时，核对记录与脚本版本的一致性。
- 用户要求草稿、暂不升版或先不改文档时，遵循该限制。“暂不提交”本身不触发收尾；若同时明确要求整理版本或文档，仍完成已授权部分。

## 收尾规则

只整理本次范围内受影响脚本的最终改动。每个脚本在一次收尾中最多递增一次版本，最多新增一个 CHANGELOG 版本小节，除非用户要求拆分版本。

### 版本与元信息

有代码改动的脚本按最终变化递增：bug 修复升补丁版本，新增功能或兼容页面升次版本，不兼容改动升主版本。已有本次收尾版本时不重复递增；无需升版时说明原因。

核对 userscript 头部的 `@name`、`@description`、`@version`、`@match` / `@include`、`@grant`、`@require`、`@connect`、`@license` 与实际用途一致。

### README.md

出现以下变化时，更新对应脚本分区：主要功能、配置项、兼容页面、存储结构、导入导出格式、迁移、安装或使用注意事项；修复使原有描述失实时也应更新。

仅内部实现、性能、注释或格式变化，且不影响上述说明时，无需更新 README，并在交付时简述原因。

### CHANGELOG.md

在对应脚本分区顶部新增与 `@version` 一致的版本小节，用简洁项目符号概括本阶段最终改动。沿用以下结构，不使用全局 `[Unreleased]` 分区，也不为未修改脚本添加记录：

```md
## 脚本名称

文件：[脚本文件名.js](./脚本文件名.js)

### x.y.z

- 修复……
- 新增……
```

### 新增、删除或重命名脚本

新增脚本在开发阶段即可补齐运行所需的 userscript 元信息；收尾时补齐 README 列表、用途、主要功能、配置与适配页面，以及 CHANGELOG 初始版本记录。

删除或重命名脚本时，收尾阶段同步整理 README 列表和分区、CHANGELOG 文件链接或删除记录，并核对 `@name` 和旧文件名引用。

### 差异检查

收尾时检查 `git diff -- README.md CHANGELOG.md` 和 `git diff -- '*.js'`；若有已暂存或新增文件，也检查相应内容。确认版本与记录一致、README 判断有依据、元信息准确，且没有无关改动。

## 交付说明

简要说明修改文件、主要结果、验证方式和未验证部分，不机械复述全部规则。

脚本开发阶段说明尚未更新版本及 README、CHANGELOG，因为本次未要求收尾；收尾阶段说明三者的更新情况及必要原因。仅文档修改说明文档结果即可。工作未完成时，明确剩余问题和所需条件。
