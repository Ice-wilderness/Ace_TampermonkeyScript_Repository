# AGENTS.md

本仓库是自用油猴 / 用户脚本合集。每个 `.js` 脚本通常独立维护，`README.md` 和 `CHANGELOG.md` 按脚本分区记录。

AI 修改本仓库时，先判断当前需求属于哪个脚本，再决定是否需要同步版本、文档和 userscript 元信息。默认小改动直接处理，中大型任务才使用 planning-with-files。

## 基本原则

- 本仓库是多脚本工具库，不是单一应用。除非用户明确要求跨脚本维护，否则不要假设一个脚本的需求会影响其他脚本。
- 如果用户只指定某一个脚本，默认只修改该脚本；可以读取其他脚本作为参考，但不得顺手修改。
- 所有改动应保持小而可验证：不添加无关功能，不做无关重构，不扩大权限或适配范围。
- 修改前先识别目标脚本、允许修改范围、是否处于收尾 / 提交 / 发布阶段。
- 如果发现旧 `task_plan.md`、`findings.md`、`progress.md` 与当前需求无关，不要复用；提醒用户归档或重置三者，而不是只处理其中一个。

## 版本与文档

开发阶段不得因为小修改、修复、调试或重构就自动更新 `@version`、`README.md` 或 `CHANGELOG.md`。

只有出现以下任一情况，才进入版本与文档收尾：

- 用户明确表示工作完成，例如“收尾一下”“整理最终版本”“准备发布”“可以提交了”。
- 用户明确要求提交或发布，例如“创建 commit”“更新版本并提交”“发版”。
- 用户明确要求更新文档或版本，例如“更新 README”“更新 CHANGELOG”“递增版本号”。

| 场景 | `@version` | `README.md` | `CHANGELOG.md` |
| --- | --- | --- | --- |
| 普通开发、调试、试改 | 不更新 | 不更新，除非用户明确要求 | 不更新 |
| 明确收尾、提交、发布 | 修改过脚本则检查并按需递增一次 | 用户可见行为变化时更新对应分区 | 更新对应脚本分区 |
| 仅修改文档、AGENTS 或注释 | 不更新脚本版本 | 按用户需求更新 | 按用户需求更新 |
| 用户要求暂不提交、只做草稿、先不改文档 | 不更新 | 不更新 | 不更新 |

一次明确收尾或提交动作中，每个被修改脚本最多递增一次版本号，并在 `CHANGELOG.md` 对应脚本分区最多新增一个版本小节，除非用户明确要求拆分版本。

## 收尾检查

进入收尾、提交、发布或版本记录阶段时，必须检查受影响脚本的 userscript 头部元信息是否准确：

- `@name`
- `@description`
- `@version`
- `@match` / `@include`
- `@grant`
- `@require`
- `@connect`
- `@license`

如果新增、删除或调整 GM API，必须检查 `@grant`。如果新增或改变适配网站、页面路径、URL 规则，必须检查 `@match` / `@include`，并在需要时同步更新 `README.md` 的兼容页面说明。

版本号建议遵循语义化版本：

- 修复 bug：递增补丁版本，例如 `3.1.14` -> `3.1.15`。
- 新增功能或兼容新页面：递增次版本，例如 `3.1.14` -> `3.2.0`。
- 大幅重构、删除功能、数据结构不兼容：递增主版本，例如 `3.1.14` -> `4.0.0`。

## README.md 规则

只有在用户明确要求收尾、提交、发布，或明确要求更新 README 时，才判断并更新 `README.md`。

收尾阶段遇到以下用户可见变化时，应更新对应脚本分区：

- 新增、删除或重命名脚本。
- 新增、删除或明显改变主要功能。
- 新增、删除或改变配置项。
- 新增、删除或改变兼容页面。
- 改变数据存储结构、导入导出格式、迁移方式、安装方式或注意事项。
- 修复的问题会影响 `README.md` 中现有描述的准确性。

仅内部实现修复、无行为变化的重构、性能优化、注释 / 日志 / 格式调整，通常不需要更新 `README.md`。若收尾阶段判断不需要更新，最终回复应说明：`README.md` 未更新，因为本次改动不影响功能说明、配置项、兼容页面或使用方式。

## CHANGELOG.md 规则

本仓库的 `CHANGELOG.md` 按脚本分区维护，不使用统一的全局 `[Unreleased]` 分区。

收尾阶段更新规则：

1. 找到被修改脚本对应的分区。
2. 在该脚本分区顶部新增一个版本小节。
3. 版本号与脚本头部 `@version` 保持一致。
4. 用简洁项目符号概括本阶段最终完成后的完整改动。
5. 不要把某个脚本的更新写到其他脚本分区。
6. 未修改脚本不要新增“暂无本次更新”。

推荐格式：

```md
## 脚本名称

文件：[脚本文件名.js](./脚本文件名.js)

### x.y.z

- 修复……
- 新增……
- 优化……
```

如果当前收尾或提交范围涉及多个脚本，每个被修改脚本都应独立判断版本递增类型，并在各自 CHANGELOG 分区记录。

## 新增、删除与重命名脚本

新增 `.js` 油猴脚本时，开发阶段可以先只创建和修改脚本文件。只有用户明确要求完成、收尾、提交或发布时，才必须补齐：

- `README.md` 脚本列表链接和对应脚本分区。
- 脚本用途、主要功能、配置项和适配页面说明。
- `CHANGELOG.md` 脚本分区和初始版本记录。
- 完整且准确的 userscript 元信息。

删除或重命名脚本时，也只在明确收尾、提交或发布阶段同步整理文档：

- 更新或移除 `README.md` 中的脚本列表和对应分区。
- 更新 `CHANGELOG.md` 中对应分区的文件链接或删除记录。
- 检查脚本头部 `@name` 是否需要同步调整。
- 检查是否仍有文档链接指向旧文件名。

## planning-with-files

本仓库可以使用 planning-with-files，但不得对所有任务强制使用。

适合使用：

- 为某个脚本新增完整功能。
- 重构某个脚本的大段逻辑。
- 排查复杂 bug。
- 修改会影响多个模块或多个代码区域。
- 需要跨多轮会话持续推进。
- 需要同时维护代码、README、CHANGELOG。
- 需要阅读多个脚本后制定迁移、整理或统一规范计划。

通常不需要使用：

- 改一两行代码。
- 修正文案、注释或日志。
- 修改很小的选择器。
- 查询代码含义。
- 单点 bug 修复且无需复杂分析。
- 用户明确要求“直接改”“不要建计划”“不要更新 planning 文件”。

使用 planning-with-files 时，每轮计划必须独立于旧任务，并明确：

- 目标脚本。
- 允许修改的文件范围。
- 是否允许更新 README。
- 是否允许更新 CHANGELOG。
- 是否需要更新版本号。
- 是否需要等待用户确认后再改代码。

planning 文件包括 `task_plan.md`、`findings.md`、`progress.md`，三者必须作为一组保留、归档、删除或重置。推荐归档到：

```text
planning-archive/
  20260507-task-name/
    task_plan.md
    findings.md
    progress.md
```

任务完成后，最终回复应说明当前计划是否完成、三个 planning 文件是否仍适合保留，以及下一个无关需求是否建议归档它们。不要自动删除 planning 文件，除非用户明确要求。

## 代码修改注意事项

修改油猴脚本时，应尽量保持：

- 不破坏已有用户数据，不随意更改 GM 存储 key。
- 不随意删除导入、导出、备份或恢复逻辑。
- 不引入不必要的外部依赖。
- 不扩大 `@match` 范围，除非本次任务明确需要。
- 不增加多余权限，尤其是 `@grant`、`@connect`。
- 对 SPA 页面、懒加载内容、滚动加载内容保持谨慎，避免重复绑定事件或重复插入元素。
- 对已有选择器、MutationObserver、定时器和缓存逻辑注意性能影响。
- 修改 UI 时避免强依赖脆弱 DOM 结构，优先使用稳定选择器或降级逻辑。
- 修改网络请求逻辑时注意跨域权限、请求频率、错误处理和站点反爬限制。
- 修改数据结构时考虑旧数据迁移或兼容读取。
- 修改导入 / 导出逻辑时考虑旧格式兼容、异常文件处理和备份提示。

## 多脚本协同

如果用户明确要求跨脚本统一维护，例如统一代码风格、统一配置面板、统一日志前缀、统一导出格式，应先列出涉及脚本，再分脚本实施。

跨脚本修改时：

- 不要把多个脚本的版本号强行统一。
- 每个被修改脚本独立判断版本递增类型。
- 每个被修改脚本在收尾阶段都有各自的 CHANGELOG 分区记录。
- README 只更新受影响的脚本分区。
- 不要顺手重构未列入范围的脚本。
- 不要因为读取了某个脚本作为参考，就把它视为已修改脚本。

## 提交前检查

只有在用户明确要求收尾、提交、发布或更新版本记录时，才执行提交前版本与文档检查。

必须检查：

```bash
git diff -- README.md CHANGELOG.md
git diff -- '*.js'
```

如果修改了脚本，还必须确认：

- 对应脚本的 `@version` 已在本次收尾阶段统一更新一次，或明确说明为什么不需要更新。
- 没有因为中间多次代码编辑而重复递增版本号。
- `CHANGELOG.md` 中对应脚本分区只为本次收尾或提交新增一个版本小节，除非用户明确要求拆分版本。
- `README.md` 是否需要更新已经判断过。
- userscript 头部元信息仍然准确。
- 没有无关格式化或无关脚本改动。
- 没有把旧 planning 文件误当成当前任务成果。
- 如果使用了 planning-with-files，已说明 `task_plan.md`、`findings.md`、`progress.md` 是否建议归档。

## 最终回复

普通开发阶段的最终回复应包含：

- 修改了哪些脚本。
- 做了哪些代码改动。
- 是否运行了测试或验证。
- 明确说明：未更新 `@version`、`CHANGELOG.md`、`README.md`，因为用户尚未明确要求收尾、提交或发布。

planning-with-files 阶段还应包含：

- 当前计划完成到哪一步。
- 是否更新了 `task_plan.md`、`findings.md`、`progress.md`。
- 如果任务已经完成，是否建议归档这三个 planning 文件。
- 如果任务未完成，下一步应继续做什么。

收尾 / 提交 / 发布阶段的最终回复应包含：

- 修改了哪些脚本。
- 是否更新了 `README.md`。
- 是否更新了 `CHANGELOG.md`。
- 是否更新了脚本 `@version`。
- 如果 `README.md` 未更新，说明原因。
- 如果未运行测试或无法验证，应明确说明。
- 如果使用了 planning-with-files，应说明三个 planning 文件是否建议归档。

## 默认行为

- 小改动直接处理，不创建 planning 文件。
- 中大型任务才使用 planning-with-files。
- 每个需求独立判断计划范围。
- 每个脚本独立维护版本和 CHANGELOG。
- 开发中不更新版本号。
- 开发中不更新 CHANGELOG。
- 开发中不主动更新 README。
- 收尾、提交或发布时再统一检查版本、文档和 userscript 元信息。
- 旧 planning 文件不得自动污染新需求。
