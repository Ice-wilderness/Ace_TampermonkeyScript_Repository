# AGENTS.md

本仓库是自用油猴 / 用户脚本合集。每个 `.js` 脚本独立维护，README.md 和 CHANGELOG.md 均按脚本分区记录。

AI 在修改本仓库时，必须优先判断“当前改动属于哪个脚本”，再决定是否需要同步更新文档和脚本头部元信息。

## 核心原则：版本与文档只在明确收尾时更新

AI 在开发过程中，不得因为每一次小修改、修复、重构、调试或阶段性代码编辑，就自动更新版本号、CHANGELOG.md 或 README.md。

只有在以下任一情况发生时，才允许统一检查并更新版本号@version、CHANGELOG.md、README.md 和 userscript 元信息：

1. 用户明确表示当前这段工作已经完成，例如：
   - “这个功能完成了”
   - “本次任务结束”
   - “收尾一下”
   - “整理最终版本”
   - “准备发布”
   - “准备提交”
   - “可以提交了”
   - “把这次改动记录一下”

2. 用户明确要求提交到 Git 仓库，例如：
   - “提交到仓库”
   - “创建 commit”
   - “准备 git commit”
   - “更新版本并提交”
   - “发版”
   - “发布新版本”

3. 用户明确要求更新文档或版本，例如：
   - “更新 README”
   - “更新 CHANGELOG”
   - “递增版本号”
   - “补一下版本记录”

除上述情况外，即使 AI 已经完成了一次代码修改，也不得主动递增版本号，不得主动追加 CHANGELOG，不得主动更新 README，除非 README 的修改本身就是用户当前明确要求的一部分。

换言之：

- 普通开发、调试、试改、局部修复阶段，只修改代码。
- 不要把每一次小工作都视为一次发布。
- 不要每改一次代码就递增 `@version`。
- 不要每改一次代码就向 `CHANGELOG.md` 追加记录。
- 不要每改一次代码就同步改 README。
- 只有在用户明确要求“完成 / 收尾 / 提交 / 发布 / 记录版本”时，才进行版本和文档收口。

## 仓库结构约定

- 每个油猴脚本通常是一个独立的 `.js` 文件。
- `README.md` 用于说明脚本用途、主要功能、配置项、兼容页面、安装方式和维护说明。
- `CHANGELOG.md` 按脚本分区记录每个脚本的版本更新。
- 新增脚本时，只有在用户明确要求完成、收尾、提交或发布时，才必须同时更新 `README.md` 和 `CHANGELOG.md`。
- 修改已有脚本时，如果进入版本与文档收尾阶段，应只更新该脚本对应的 README 分区和 CHANGELOG 分区，不要无关改动其他脚本说明。

## 修改脚本时的文档规则

AI 在修改 `.js` 油猴脚本的过程中，不要因为每一次中间代码编辑、修复或重构就立即更新版本号、CHANGELOG 或 README。

版本号、CHANGELOG、README 和 userscript 元信息只应在用户明确要求“当前工作完成”“收尾”“提交到 Git 仓库”“发布版本”或“更新文档 / 版本记录”时，统一检查和更新一次。

也就是说：

- 中间开发阶段可以多次修改脚本代码。
- 中间开发阶段不得反复递增 `@version`。
- 中间开发阶段不得反复向 `CHANGELOG.md` 追加记录。
- 中间开发阶段不得主动更新 README，除非用户当前任务明确要求修改 README。
- 一次明确收尾或提交动作，最多只应产生一个版本号递增结果，除非用户明确要求拆分为多个独立版本。
- 一次明确收尾或提交动作，最多只应在对应脚本的 `CHANGELOG.md` 分区新增一个版本小节，用于概括该阶段的完整改动。

在用户明确要求收尾、提交、发布或更新版本记录时，必须检查是否需要更新：

- `README.md`
- `CHANGELOG.md`
- 脚本文件头部的 userscript 元信息

尤其要检查以下 userscript 元信息是否仍然准确：

- `@name`
- `@description`
- `@version`
- `@match`
- `@include`
- `@grant`
- `@require`
- `@connect`
- `@license`

如果新增、删除或调整了脚本使用的 GM API，必须同步检查 `@grant`。

如果新增或改变了适配网站、页面路径、URL 规则，必须同步检查 `@match` / `@include`。只有在进入明确收尾、提交、发布或用户要求更新 README 时，才同步更新 `README.md` 的兼容页面说明。

## README.md 更新规则

只有在用户明确要求收尾、提交、发布，或明确要求更新 README 时，才判断并更新 `README.md`。

当改动影响用户理解、安装或使用脚本时，应更新 `README.md` 中对应脚本分区。

以下情况在收尾或提交阶段必须更新 `README.md`：

- 新增脚本。
- 删除脚本。
- 脚本改名或文件名变化。
- 新增、删除或明显改变主要功能。
- 新增、删除或改变配置项。
- 新增、删除或改变兼容页面。
- 改变数据存储结构、导入导出格式或迁移方式。
- 改变脚本使用方式、安装方式或注意事项。
- 修复的问题会影响 `README.md` 中现有描述的准确性。

以下情况通常不需要更新 `README.md`：

- 仅修复内部实现细节。
- 仅重构代码但不改变用户可见行为。
- 仅优化性能但不改变功能描述。
- 仅调整注释、日志或代码格式。
- 仅修复 `README.md` 中没有提到的边缘 bug。
- 当前仍处于中间开发阶段，用户尚未要求收尾、提交或发布。

如果在收尾或提交阶段判断 `README.md` 不需要更新，最终回复中应说明：

> README.md 未更新，因为本次改动不影响功能说明、配置项、兼容页面或使用方式。

## CHANGELOG.md 更新规则

不得在开发过程中的每一次中间代码编辑后更新 `CHANGELOG.md`。

只有在用户明确要求当前工作完成、收尾、提交到 Git 仓库、发布版本或更新版本记录时，才更新 `CHANGELOG.md` 中对应脚本分区。

本仓库的 `CHANGELOG.md` 按脚本分区维护，不使用统一的全局 `[Unreleased]` 分区。

更新规则：

1. 找到被修改脚本对应的分区。
2. 在该脚本分区顶部新增一个版本小节。
3. 该版本小节应概括本阶段最终完成后的完整改动。
4. 版本号应与脚本文件头部 `@version` 保持一致。
5. 用简洁项目符号说明本次改动。
6. 不要把某个脚本的更新写到其他脚本分区。
7. 未修改的脚本分区不要新增“暂无本次更新”。
8. 同一次明确收尾或提交动作中，不要因为多次中间代码修改而新增多个版本小节，除非用户明确要求拆分版本。
9. 如果用户尚未明确要求收尾、提交、发布或更新版本记录，则不要更新 `CHANGELOG.md`。

推荐格式：

```md
## 脚本名称

文件：[脚本文件名.js](./脚本文件名.js)

### x.y.z

- 修复……
- 新增……
- 优化……
````

如果当前收尾或提交范围涉及多个脚本，则每个被修改脚本都应在 `CHANGELOG.md` 中新增对应版本记录。

## 版本号规则

修改油猴脚本时，不要在每一次中间代码编辑后递增版本号。

版本号只应在用户明确要求“完成 / 收尾 / 提交 / 发布 / 更新版本记录”时统一递增一次。

如果本阶段最终要提交，并且修改了某个 `.js` 油猴脚本，则必须检查并更新该脚本头部的 `@version`，同时在 `CHANGELOG.md` 对应脚本分区记录同一个版本号。

建议采用语义化版本：

* 修复 bug：递增补丁版本，例如 `3.1.14` -> `3.1.15`
* 新增功能或兼容新页面：递增次版本，例如 `3.1.14` -> `3.2.0`
* 大幅重构、删除功能、数据结构不兼容：递增主版本，例如 `3.1.14` -> `4.0.0`

如果同一次收尾或提交中既有 bug 修复又有小功能新增，通常按影响最大的类型递增一次版本号。

如果只是修改 `README.md`、`CHANGELOG.md`、`AGENTS.md` 或注释，不需要更新脚本版本号。

如果用户明确要求“暂不提交”“只做草稿修改”“只探索实现方案”“先不要更新版本”“先不要改文档”，则不得主动递增版本号，不得更新 CHANGELOG，除非用户之后另有明确要求。

## 新增脚本规则

新增 `.js` 油猴脚本时，开发阶段可以先只创建和修改脚本文件。

只有在用户明确要求完成、收尾、提交或发布时，才必须同时完成：

* 在 `README.md` 的“脚本列表”中加入链接。
* 在 `README.md` 中新增该脚本分区。
* 说明脚本文件名、用途、主要功能、配置项和适配页面。
* 在 `CHANGELOG.md` 中新增该脚本分区。
* 在 `CHANGELOG.md` 中记录初始版本。
* 检查脚本头部 userscript 元信息是否完整。

如果用户只是要求“先写一个脚本草稿”“先实现一下”“先试试”，不得主动更新 README、CHANGELOG 或额外递增版本记录。

## 删除或重命名脚本规则

删除脚本时，只有在用户明确要求收尾、提交或发布时，才同步整理文档：

* 从 `README.md` 的脚本列表中移除。
* 删除或调整 `README.md` 中对应脚本分区。
* 在 `CHANGELOG.md` 对应分区记录删除原因，或在仓库文档中说明该脚本已移除。
* 检查是否有其他文档仍链接到旧文件名。

重命名脚本时，只有在用户明确要求收尾、提交或发布时，才同步整理文档：

* 更新 `README.md` 中的文件链接。
* 更新 `CHANGELOG.md` 中的文件链接。
* 检查脚本头部 `@name` 是否需要同步调整。
* 确保旧文件名不会继续出现在文档链接中。

## 代码修改注意事项

修改油猴脚本时，应尽量保持：

* 不破坏已有用户数据。
* 不随意更改 GM 存储 key。
* 不随意删除导入、导出、备份或恢复逻辑。
* 不引入不必要的外部依赖。
* 不扩大 `@match` 范围，除非本次任务明确需要。
* 不增加多余权限，尤其是 `@grant`、`@connect`。
* 对 SPA 页面、懒加载内容、滚动加载内容的处理要谨慎，避免重复绑定事件或重复插入元素。
* 对已有选择器、MutationObserver、定时器和缓存逻辑的修改要注意性能影响。

## 提交前检查

只有在用户明确要求收尾、提交、发布或更新版本记录时，才执行提交前版本与文档检查。

在完成提交前检查时，必须检查：

```bash
git diff -- README.md CHANGELOG.md
git diff -- '*.js'
```

如果修改了脚本，还必须确认：

* 对应脚本的 `@version` 已在本次收尾阶段统一更新一次，或明确说明为什么不需要更新。
* 没有因为中间多次代码编辑而重复递增版本号。
* `CHANGELOG.md` 中对应脚本分区只为本次收尾或提交新增一个版本小节，除非用户明确要求拆分版本。
* `README.md` 是否需要更新已经判断过。
* userscript 头部元信息仍然准确。
* 没有无关格式化或无关脚本改动。

如果用户尚未明确要求收尾、提交、发布或更新版本记录，则不要执行版本号递增、CHANGELOG 追加或 README 收口更新。

## 最终回复要求

### 普通开发阶段的最终回复

如果用户只是要求修改、修复、实现、调试或试验代码，但没有明确要求收尾、提交、发布或更新版本记录，最终回复应包含：

* 修改了哪些脚本。
* 做了哪些代码改动。
* 是否运行了测试或验证。
* 明确说明：未更新 `@version`、`CHANGELOG.md`、`README.md`，因为用户尚未明确要求收尾、提交或发布。

### 收尾 / 提交 / 发布阶段的最终回复

如果用户明确要求当前工作完成、收尾、提交、发布或更新版本记录，最终回复应包含：

* 修改了哪些脚本。
* 是否更新了 `README.md`。
* 是否更新了 `CHANGELOG.md`。
* 是否更新了脚本 `@version`。
* 如果 `README.md` 未更新，说明原因。
* 如果未运行测试或无法验证，应明确说明。