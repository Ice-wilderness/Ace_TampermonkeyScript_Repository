# 更新日志

本文件按脚本分区记录更新。新增脚本时，请增加新的脚本分区。

各脚本分区使用 Markdown 内嵌 HTML 的 `<details>` 折叠块；在支持该语法的阅读器中，可点击脚本名展开查看。

## 目录

- [ChatGPT 图片批量下载器](#chatgpt-image-batch)
- [Bilibili 视频观看历史记录](#bilibili-history)
- [bilibili 查看关注时间](#bilibili-follow-time)
- [Discuz 论坛帖子已读标记与图片预览](#discuz-read-image-preview)
- [自用论坛辅助签到](#forum-auto-signin)
- [通用请求抓包记录器](#universal-api-capture)

<a id="chatgpt-image-batch"></a>
<details>
<summary><strong>ChatGPT 图片批量下载器</strong></summary>

文件：[ChatGPT 图片批量下载器.js](./scripts/ChatGPT%20图片批量下载器.js)

### 1.0.1

- 优化批量下载失败后的重试体验：若部分图片失败，会自动取消成功图片勾选，仅保留失败图片，便于直接再次下载。

### 1.0.0

- 新增 ChatGPT 图片批量下载器脚本，可在 ChatGPT 图片页面勾选多张图片后批量下载。
- 支持右下角批量下载面板、全选/取消全选和已选数量显示。
- 批量下载时通过 ChatGPT 页面原生保存/下载按钮逐张触发下载。

</details>

<a id="bilibili-history"></a>
<details>
<summary><strong>Bilibili 视频观看历史记录</strong></summary>

文件：[Bilibili视频观看历史记录.js](./scripts/Bilibili视频观看历史记录.js)

### 3.5.0

- 设置页新增“悬浮按钮隐藏规则”下拉配置，可选择不隐藏、隐藏视频页进度悬浮按钮、隐藏非视频页脚本设置按钮或全部隐藏。
- 保存设置或恢复默认设置后会立即刷新当前页面的悬浮按钮显示状态。

### 3.4.0

- 历史页新增悬浮同步按钮，可将当前已加载的 Bilibili 官方观看历史同步到脚本本地记录。
- “继续同步”会滚动到页面底部并等待更多历史卡片加载，便于逐批同步其他设备观看记录。
- 同步时按更长观看进度合并记录，进度差距在 5 个百分点以内则跳过且不更新时间。
- 支持从历史卡片链接解析分 P 参数，尽量精确同步到对应分 P 记录。

### 3.3.0

- 历史管理新增“保留 N 天/月/年”清理入口，可删除早于保留范围的久远历史记录。
- 清理前增加二次确认，明确保留周期、删除时间范围和预计删除数量。
- 批量删除改为按分片批量写入并显示删除进度，减少大量记录删除时的浏览器卡顿。
- 设置面板内的提示消息移到屏幕中上方并提高层级，避免被灰色蒙版遮住。

### 3.2.4

- 修复头部动态弹窗中部分视频封面标签未按小尺寸样式显示，导致“已观看100%”等文本换行溢出的问题。

### 3.2.3

- 脚本设置面板默认显示"设置"tab，菜单入口、左下角快捷入口同步调整为设置页。
- 历史管理搜索框不再因输入而失焦：工具栏与结果区拆分渲染，搜索仅局部刷新结果区。
- 统计页新增可视化图表：环形完成度、状态/进度分布条形图、近期趋势柱状图（支持 7/30/90 天切换）。
- 修复统计完成度虚高：完成度分母改为全部记录（含已访问），分子为高进度。
- 修复趋势图日期使用 UTC 导致东八区凌晨记录跨天偏移。
- 合并统计页两次全量扫描为单次遍历，消除重复性能开销。
- 历史管理默认每页 20 条，首屏异步加载避免全量阻塞工具栏渲染。
- 历史管理工具栏重新布局：搜索独占一行，筛选与操作按钮分行排列。
- 趋势图柱形上方显示具体数量标签，全选框支持 checked/indeterminate 状态。
- 搜索框右侧新增清空按钮，统计页移除"查看未看完"快捷按钮。

### 3.2.2

- 收敛调试日志级别：将续播跳转、过期待跳转进度和视频 ID 初始化轮询等预期流程从 warning 降为普通日志。
- 普通卡片/选集处理分支的慢日志不再写入历史 warning/error 日志，减少历史异常导出的噪声。

### 3.2.1

- 优化调试日志导出：当前页面保留完整日志，历史页面仅保留 warning/error 级异常日志，避免普通日志跨页面刷屏。
- 开启调试日志时捕获页面 `error`、`unhandledrejection` 以及控制台 `warn/error`，并对相同异常合并计数，便于排查脚本冲突。
- 异常日志写入改为节流批量保存，并增加安全序列化、Hook 防递归、console Hook 失败静默降级和卸载阶段轻量 flush。
- 设置页新增“清空调试日志”按钮，仅清理调试日志，不影响观看历史记录。

### 3.2.0

- 优化存储层：新增持久化 `bvh_base_index`（base→shard 索引），`getRelatedKeys` 按需加载目标分片替代遍历 64 分片，大幅减少冷启动 GM 读取次数。
- 新增 `bvh_storage_revision` 版本号，多标签页切回时基于版本号判断是否需要同步，避免盲目全量刷新缓存。
- 优化 `EpisodeResolver._collectItems()` 缓存策略，同一同步周期内复用 DOM 扫描结果，消除 `getItems()` 内的重复 `querySelectorAll`。
- 消除初始加载时 `scanExistingLinks` 与 `observeContentRoot` 对同一 root 的双重扫描。
- `SettingsManager.save` 仅在显示相关配置变更时才触发 DOM 重绘，避免无关设置保存导致全量刷新。
- 播放进度常规保存改为仅写 GM 分片存储，localStorage 备份仅在 pause/beforeunload 或异常兜底时写入。
- 新增 `StorageManager.importRecords` 批量导入 API，defer flush/revision/index-persist 到最后一次性提交，减少大量 GM 写入。
- 修复 `deleteRecord` 中跨分片 base 索引误删除的潜在缺陷。
- 修复已有 v3 用户首次升级后 `_lastKnownRevision` 未初始化导致首次切回误判 stale 的问题。
- 路由切换时去除冗余 `refreshPlaylistItems` 调用，由 `refreshForRoute` 统一处理。
- 历史管理面板 `getFilteredRows` 在单次 render 周期内复用过滤结果。

- 修复播放页加载时全局 DOM 监听可能持续抢占主线程，导致视频页无法加载或浏览器卡死的问题。
- 将视频卡片和选集标记改为容器级监听与分批处理，保留推荐、分 P、合集、动态、搜索、历史、稍后再看和头部弹窗标记能力。

### 3.1.26

- 修复头部收藏弹窗从其他收藏夹切换到“稍后再看”时，卡片链接变为 `/list/watchlater?...&bvid=...` 导致标记未刷新或沿用旧标记的问题。

### 3.1.25

- 适配 B 站新版头部动态、历史、收藏弹窗的视频卡片结构，恢复已观看标签和进度条显示。
- 扩展头部弹窗卡片刷新逻辑，避免收藏夹切换等节点复用场景沿用旧标记。

### 3.1.24

- 优化播放页推荐封面悬停时的 DOM 变更处理，提前跳过无关临时节点，减少 MutationObserver 空转。
- 调整调试日志输出策略，仅在 MutationObserver 批次发现真实处理对象或耗时异常时记录，避免调试模式下被无关悬停事件刷屏。

### 3.1.23

- 降低播放页 DOM 监听压力，全局 MutationObserver 改为只监听节点增删，避免播放器属性高频变化导致脚本空转。
- 修复同一视频仅追加 `vd_source` 等查询参数时会重复销毁并初始化播放器监听的问题。
- 避免刚开始播放时保存 `00:00` 进度并触发无意义的续播提示。

### 3.1.22

- 新增设置页“下载调试日志”按钮，可导出脚本自身的 BvH 调试日志、环境信息和关键计数，便于用户反馈卡死或自动跳转问题。

### 3.1.21

- 增强调试日志，开启“调试日志”后会输出启动、DOM 监听、播放页初始化、续播跳转、存储分片读取和进度保存等关键路径信息。
- 增加关键耗时统计与慢操作告警，便于排查进入播放页卡死或疑似自动刷新问题。

### 3.1.20

- 修复首次进入无历史记录的视频页时只显示“脚本设置”按钮，未立即显示左下角已访问标签的问题。

### 3.1.19

- 修复 3.1.15 启动优化过早触发，可能打断 B 站头部 Vue 挂载并导致头部消失的问题。
- 调整 DOM 阶段启动策略，改为头部节点出现后等待子节点变化稳定，再注入脚本 UI 和启动页面扫描。

### 3.1.18

- 修复播放器“选集”浮层中长标题会把观看记录标签挤出可视范围的问题。
- 选集浮层标签改为固定显示在条目右侧，并为标题预留空间。

### 3.1.17

- 继续增强普通视频播放器“选集”浮层标签匹配逻辑。
- 当标题反查失败时，按播放器选集与右侧视频列表的同序关系兜底匹配真实 BV 号。

### 3.1.16

- 修复普通视频播放器“选集”浮层中无法显示观看记录标签的问题。
- 播放器选集项现在会按右侧合集/选集列表标题反查真实 BV 号，避免误按当前视频分 P 匹配记录。

### 3.1.15

- 优化脚本 DOM 阶段启动逻辑，改为等待 B 站头部节点挂载后尽快启动，并保留超时兜底。
- 移除固定 2.5 秒启动延迟，减少页面已就绪时的无谓等待。

### 3.1.14

- 修复 Bilibili `/list/` 投稿播放列表缺少观看记录标记的问题。
- 投稿播放列表现在按每个视频自身 BV 号匹配记录，并在封面上显示观看状态标签。
- 优化播放列表项刷新逻辑，降低滚动加载、懒加载封面和 SPA 切换后漏标的概率。

### 3.1.13

- 移除“导入当前官方历史页”功能，减少低频功能带来的维护和误导入风险。
- 历史管理改为分页展示，默认每页 50 条，可切换 30、50、100 条。
- 优化设置页 UI，将配置拆分为“显示与提示”和“标签样式”两个区域。
- 优化管理面板视觉层级、间距、输入框、按钮和分页控件样式。

</details>

<a id="bilibili-follow-time"></a>
<details>
<summary><strong>bilibili 查看关注时间</strong></summary>

文件：[bilibili查看关注时间.js](./scripts/bilibili查看关注时间.js)

### 1.1.2

- 视频播放相关页面继续保留用户悬浮名片关注时间标签，但不再触发关注列表全量同步，降低对播放器和观看历史脚本的影响。

### 1.1.0

- 新增 Bilibili 关注时间查看脚本。
- 支持在关注/粉丝列表、用户主页和全站用户悬浮名片中显示关注时间。
- 支持按登录账号缓存关注时间数据，并在缓存缺失或过期时自动同步。

</details>

<a id="discuz-read-image-preview"></a>
<details>
<summary><strong>Discuz 论坛帖子已读标记与图片预览</strong></summary>

文件：[Discuz 论坛帖子已读标记与图片预览.js](./scripts/Discuz%20论坛帖子已读标记与图片预览.js)

### 4.6.3

- 修复图片尚未完全加载时打开灯箱，切换上下张只更新页码但画面仍停留在旧图的问题。
- 灯箱切图增加加载态和过期加载保护，避免慢图或快速切换时显示错位。

### 4.6.2

- 全面重构代码结构：提取 normalizeRecord 辅助函数、配置注册表 CONFIG_DEFS、设置面板 UI 工厂函数。
- 预览模块拆分为独立模块级函数，引入 PreviewState 类 + WeakMap<threadElement> 管理状态与 DOM 引用。
- 预览图片点击改为容器级事件委托，减少独立监听器创建/销毁开销。
- CSS 变量化暗黑模式适配，替代重复的硬编码颜色覆盖选择器。
- 修复重构中 createStyleSelect 返回字段名不匹配导致设置面板“已读样式”下拉框不显示的问题。
- 修复 syncFullButtonSize 的 rAF 防抖状态为全局共享导致多帖子并发时高度更新遗漏的问题。
- 修复设置面板提示文案中文引号左右均为右引号“”的显示异常。

### 4.6.1

- 修复自动预览第 1 页时，前几个候选图片不满足尺寸要求会导致展示数量少于配置值的问题。
- 自动预览现在会继续从第 1 页后续候选图片中补齐，直到达到自动预览数量或第 1 页图片用完。

### 4.6.0

- 新增自动预览并发数配置，默认 1，最高 5，可按论坛响应能力提高列表页自动预览速度。
- 关闭后续分页抓取时，手动预览也保持静默，不再显示加载数量或分页进度提示。

### 4.5.2

- 优化图片预览状态文案：关闭后续分页抓取时，不再显示“已抓取 1/N 页”这类分页进度。

### 4.5.1

- 修复第 1 页图片数量超过自动预览上限时不会显示“加载更多图片”卡片的问题。
- “加载更多图片”现在会优先补完第 1 页剩余图片，只有第 1 页已加载完且开启后续分页时才继续抓取后续页。

### 4.5.0

- 新增“允许手动抓取后续分页”设置，默认只抓取帖子第 1 页图片。
- 关闭后续分页抓取时，自动预览和手动预览都不会继续请求第 2 页及之后的图片。
- 开启后续分页抓取后，预览网格末尾显示“加载更多图片”卡片，并继续按每次 3 页追加抓取。
- 优化“加载更多图片”卡片尺寸，使其自动对齐当前最后一张图片的实际高度。

### 4.4.2

- 将“查看全部图片”改为预览网格末尾的“加载更多图片”卡片，尺寸与图片项保持一致。
- 点击加载卡片时按批次追加后续 3 页图片，避免无图长帖或多页帖一次性抓取过多页面。

### 4.4.1

- 修复无图多页帖子手动查看时会持续抓取过多分页的问题，改为每次只追加抓取后续 3 页。
- 优化自动预览无图时的表现：不显示预览区域，按钮显示“无图片”，用户手动点击后再继续抓取后续分页。

### 4.4.0

- 自动预览改为静默运行，不再显示抓取进度、加载数量或自动预览失败提示。
- 自动预览不再写入“已看图”标记，只有手动预览或查看全部图片时才记录看图状态。
- 新增页面内可拖动悬浮设置按钮，可直接打开 Discuz 辅助设置面板并记住位置。

### 4.3.0

- 新增轻量设置面板，整合图片预览、已读样式和数据管理。
- 图片预览改为列表页自动抓取第 1 页，默认最多展示 5 张图片，并可手动查看全部分页图片。
- 新增已访问帖子显示样式配置，支持灰底标签、半透明、删除线、半透明加删除线、标题变色和隐藏已读帖子。
- 保持已读记录只由真实进入帖子详情页产生，自动预览和手动看图仅记录“已看图”状态。

### 4.2.1

- 移除帖子列表中的“标记已读”和“清除足迹”入口，避免误触并保持已读状态只由真实访问产生。
- 同步清理相关按钮样式、事件绑定和文档说明。

### 4.2.0

- 图片预览改为默认抓取前 3 页，并支持继续抓取后续每 3 页。
- 优化分页 URL 构造、图片高清地址提取和预览缓存结构。
- 新增单帖足迹操作，可单独标记已读或清除此帖记录。
- 数据管理面板新增足迹统计、最近记录时间和当前存储 key。
- 增强异步加载帖子列表的按钮补扫与状态标记刷新。

</details>

<a id="forum-auto-signin"></a>
<details>
<summary><strong>自用论坛辅助签到</strong></summary>

文件：[自用论坛辅助签到自写.js](./scripts/自用论坛辅助签到自写.js)

### 2.11.1

- 优化“签到动作完成后自动关闭站点页面”逻辑，支持跨跳转保留本次签到动作的待关闭状态。
- SS同盟、老王论坛和 ZodGame 在脚本本次提交或点击签到后，后续确认成功时会继续尝试自动关闭页面。
- 有叽叽论坛和搜书吧从控制台打开后确认成功时会尝试自动关闭；普通直接访问仍不会因已签到状态自动关闭。

### 2.11.0

- 新增老王论坛支持，可从控制台以前台方式打开签到页并点击 `k_misign` 签到入口。
- 老王论坛遇到站点点击验证时标记为需前台处理，验证完成后再由页面脚本确认签到状态。

### 2.10.1

- 进入站点页面时若今日已完成，成功 toast 会在 3 秒后自动关闭，并按站点记录当天显示次数。
- 今日已完成提示同一站点每天最多显示 3 次，达到上限后当天不再弹出。

### 2.10.0

- 新增“签到动作完成后自动关闭站点页面”配置项，脚本本次完成点击、提交或接口签到并确认成功后可自动关闭站点页。
- 已签到检测路径不会触发自动关闭，避免打开页面时发现今日已签到就误关标签页。
- 次元狗签到前增加状态预检，已签到时直接记录成功，不再继续提交签到请求。

### 2.9.1

- 修复有叽叽论坛签到接口和复查页返回 GBK 内容时被 `fetch().text()` 误解码为乱码，导致成功提示无法识别、必须手动刷新后才标记完成的问题。

### 2.9.0

- 签到成功 toast 新增“今日不再提示”按钮，可在当前站点当天隐藏后续成功提示，减少正常浏览网站时的遮挡干扰。

### 2.8.4

- 优化有叽叽论坛页面内 API 提交后的确认流程，提交后会多次无缓存拉取签到页复查，减少必须手动刷新后才标记完成的问题。
- 扩展有叽叽论坛复查判定，结合签到服务台、当前 UID 签到行和成功提示确认当日签到状态。

### 2.8.3

- 修复次元狗接口返回成功文案但实际未完成签到时被提前标记成功的问题。
- 次元狗签到请求增加防缓存参数，并在提交后多次调用状态接口复查，只有确认 `customPointSignDaily.signed` 后才记录完成。

### 2.8.2

- 修复有叽叽论坛误将排行榜或其他用户的“今日已签到”文本识别为当前账号签到成功的问题。
- 优化有叽叽论坛页面内 API 提交后的复查逻辑，提交后会重新拉取签到页并以签到服务台状态确认结果。
- 优化一键处理调度，控制台直签和前台/后台页面打开可同时执行，直签目标的失败重试不再阻塞其他直签目标启动。

### 2.8.1

- 将初音的青葱改回后台打开页面后点击寻宝按钮，避免 `getVip` / `hunt` API 访问异常影响签到。
- 新增初音的青葱寻宝结果弹窗识别，支持通过“寻宝报告”“最终携带回了…积分”和“今日已完成寻宝”等文案确认完成。
- 移除初音的青葱不再需要的跨域请求权限。

### 2.8.0

- 将有叽叽论坛改为在签到页内提交 `dsu_paulsign` API，避免从控制台跨站直签触发 Cloudflare 验证问题。
- 优化一键直签调度，支持多个控制台 API 目标并发执行，并为 South-Plus 独立任务并行请求。
- 新增控制台直签失败自动重试，失败后等待 3 秒重试，最多 3 次后才保留最后一次失败状态和调试日志。
- 新增签到页面顶部状态提示，显示签到中、成功、失败、需登录或需前台处理原因，并支持点击关闭。

### 2.7.2

- 调整签到失败调试日志的记录时间、导出时间和文件名时间，改为使用本地时区并保留时区偏移。

### 2.7.1

- 修复多个站点同时更新签到状态时，控制台可能继续显示“已打开”的问题。
- 修复初音的青葱用户状态接口已显示今日完成时，控制台直签仍因寻宝接口返回 `code:5` 被误判失败的问题。

### 2.7.0

- 新增一键处理和一键直签后的控制台自动刷新状态，默认每 2 秒刷新一次并持续约 2 分钟。
- 关闭控制台或切换到配置清单时会自动停止刷新，避免后台计时器继续重绘。
- 控制台提示区会显示自动刷新剩余时间，便于确认当前是否仍在跟踪签到结果。

### 2.6.0

- 新增签到失败调试日志，记录失败时的请求、响应和原因，并支持菜单导出最近 3 天日志。
- 增加失败前复查窗口，首次未确认成功时会继续按 1 秒 / 3 秒节奏复查，减少异步页面状态导致的误判。
- 优化绯月、司机社、有叽叽论坛和维咔的登录态与成功状态检测，修复搜索框、模板链接或过期令牌导致的误判。
- 将有叽叽论坛调整为后台打开签到页后检测页面结果，不再从控制台跨站直签。
- 改进调试日志脱敏和 GBK 页面解码，避免导出敏感 Cookie、`formhash`、`safeid` 等字段。

### 2.5.0

- 新增有叽叽论坛支持，并接入 `dsu_paulsign` 控制台直签流程。
- 有叽叽论坛直签会动态读取 `formhash`，再发送签到请求，避免写死一次性参数。
- 补充有叽叽论坛的脚本匹配规则和跨域请求权限。

### 2.4.0

- 新增控制台直签能力，已适配站点可直接从 `limestart.cn` 控制台发送签到请求并记录状态。
- 将飞雪论坛、South-Plus、夜世界、次元狗和 GalgameX 新站接入控制台直签流程。
- 将 2dfan 调整为前台验证流程，便于完成阿里云验证码后再确认签到状态。
- 新增搜书吧支持，登录访问首页后自动记录为已获得 2 银币。
- 增强签到 API 探测工具，支持跨页面跳转、请求响应、点击入口和更多请求类型的诊断捕获。

### 2.3.0

- 新增 `limestart.cn` 每日签到控制台，可集中查看启用站点的签到状态和一键打开未完成目标。
- 新增内置站点打开清单配置和自定义签到目标管理，支持后台、前台和手动打开方式。
- 新增每日状态记录、手动标记成功/失败/跳过、失败提示、需登录和需前台处理状态展示。
- 新增提醒状态下 3 秒后自动展开控制台的可选项，并在悬浮按钮显示倒计时和取消入口。
- 优化控制台搜索、滚动保持、输入焦点、移动端布局和从悬浮按钮展开到完整面板的打开动效。

### 2.2.1

- 修复司机社点击签到后未等待页面确认，导致首次签到完成后本地签到日期可能延迟到下一次访问才记录的问题。

</details>

<a id="universal-api-capture"></a>
<details>
<summary><strong>通用请求抓包记录器</strong></summary>

文件：[通用请求抓包记录器.js](./scripts/通用请求抓包记录器.js)

### 0.1.2

- 调整抓包日志条目时间、导出时间和文件名时间，改为使用本地时区并保留时区偏移。

### 0.1.1

- 新增独立通用请求抓包记录器脚本，可按站点启用并导出 JSON 日志文件。
- 支持记录 `fetch`、`XMLHttpRequest`、`sendBeacon`、普通表单提交和链接点击。
- 表单记录支持提交按钮覆盖属性、GET 表单 URL 参数、POST 请求体、同名字段数组和基础敏感字段脱敏。
- 修复 `fetch(new URL(...))` 等请求 URL 记录不准确的问题。
- 收紧表单提交按钮兜底识别，避免把普通输入框误记为 submitter。

</details>
