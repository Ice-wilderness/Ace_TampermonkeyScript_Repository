# Tasks

## 1. 配置与统一视觉

- [x] 1.1 在目标脚本添加五个外观默认值、范围与归一化，接入初始化及 SettingsManager.save；复用 harness 编写 `tools/bilibili-history/floating-entry-test.mjs`，验证旧配置补齐、逐字段异常回退、有效值持久化及其他设置保留。
- [x] 1.2 实现共享悬浮视觉辅助方法与作用域 CSS，建立明暗主题、强调色、自定义大小、背景 alpha 和两种信息模式；在同一测试中检查两类入口的样式、背景透明度不影响文本及缺失信息不生成虚构内容。
- [x] 1.3 改造 showViewPanel、showQuickEntry、updateViewPanelProgress 与 refreshFloatingButtons，保留 DOM id 和当前视频上下文，按路由统一 visibility；验证四种显示范围、无记录视频回退、进度原地更新、分 P 切换和入口唯一性。

## 2. 位置与输入交互

- [x] 2.1 让两类入口共用旧格式位置读取与边界校正，统一设置页和菜单恢复位置；补充测试验证旧坐标、损坏坐标、resize、大小与模式变更，以及两个恢复入口不影响外观配置。
- [x] 2.2 实现带 5px 阈值的指针拖拽、结束误点击抑制、取消和销毁清理；通过指针事件测试验证点击只打开一次、拖拽只保存位置、取消不提交、反复刷新不重复触发。
- [x] 2.3 增加可访问名称、键盘激活、可见焦点与减少动态效果支持；通过浏览器键盘验证 Tab、Enter、Space 和关闭面板后焦点回到入口，并检查 reduce-motion 下的样式。

## 3. 设置外观区与草稿预览

- [x] 3.1 在 HistoryManagerPanel 添加独立外观区、五个配置控件、两类入口预览及局部恢复默认；验证修改只影响预览、切换页签保留草稿、全隐藏仍能预览且局部恢复不更改位置和显示范围。
- [x] 3.2 将外观字段接入表单验证与异步保存流程，保存成功后更新真实入口；补充非法值、保存失败、放弃修改、保存后关闭及全局恢复默认测试，确认正式设置与入口在失败和放弃时保持原样。
- [x] 3.3 运行 `node tools/bilibili-history/tag-style-test.mjs` 并修复与本次设置布局变更相关的回归，确认原标签预览、显示设置和维护操作继续可用。

## 4. 综合验收

- [x] 4.1 执行 `node --check scripts/Bilibili视频观看历史记录.js`、`node tools/bilibili-history/floating-entry-test.mjs`、`node tools/bilibili-history/startup-test.mjs` 和 `node tools/bilibili-history/player-test.mjs`，记录结果并修复本次引入的问题。
- [x] 4.2 用本地浏览器夹具截图检查 1280px 与 390px 视口、明暗主题、自定义大小、两种模式、60／100 背景透明度的代表组合，验证长分 P 信息、缺失时间、复杂背景、视口边缘和键盘焦点无溢出且可辨识。
- [x] 4.3 具备目标站点环境时检查视频播放中更新、视频／非视频切换与拖拽；无环境则明确记为未实测。检查最终差异仅涉及目标脚本和必要测试、未改变权限及历史格式；交付区分静态检查、本地运行与站点结果。本阶段不升版、不改 README／CHANGELOG，除非用户另行要求收尾。

## 验证记录（2026-09-25）

- 脚本语法检查、悬浮入口专项、标签样式、启动、播放会话测试均通过；播放会话为 11/11。
- 浏览器夹具覆盖 1280px 和 390px 视口、明暗主题、自定义大小、两种模式、60/100 背景不透明度；已人工检查外观矩阵与窄屏设置预览截图。
- 真实站点未实测：browser-use 连接失败，doctor 确认未发现 DevToolsActivePort、活动浏览器连接为 0；未修改浏览器调试设置。
- 差异仅涉及目标脚本、测试工具和本变更规划记录；元信息、记录协议、版本、README、CHANGELOG 未变更。

## 5. 设置页布局反馈调整

- [x] 5.1 设置页改为四分类固定导航，桌面对应预览并排、窄屏顺序排列；已验证每次仅一类可见、预览随分类切换及分类栏无溢出。
- [x] 5.2 观看标记的封面与合集配置分别折叠，默认只展开封面；原标签样式专项测试通过。
- [x] 5.3 保留跨分类与跨页签草稿，保存错误自动切换分类并聚焦文本或数字输入；专项测试及桌面、390px 截图检查通过。
