## 1. 隔离验证基础

- [x] 1.1 在 `tools/bilibili-history/` 建立从原脚本加载实现的 Node 测试入口、内存 GM 存储和多实例调度夹具；通过启动夹具证明不会读取或修改用户真实历史。
- [x] 1.2 固化上一轮八组复现作为回归用例，明确修复前的实际结果和修复后的预期结果；运行 `node tools/bilibili-history/test.mjs` 验证用例能够捕获基线问题。
- [x] 1.3 增加合法 v1/v2/v3、无效导入、0/少量/10000 条历史及分 P 测试数据生成器；验证数据可重复生成且不依赖真实视频资料。

## 2. 可靠存储提交与兼容读取

- [x] 2.1 实现异步 GM 存储适配及唯一实例/事务身份，补齐实际调用的 GM 授权；通过缺失 API、写入拒绝和重复请求测试，并检查元信息没有新增站点或网络权限。
- [x] 2.2 实现不可变数据块、提交清单与完整性检查，将单条和批量写入接入同一提交路径；验证两个实例交错保存同分片不同视频后两条记录均可由新实例读取。
- [x] 2.3 实现旧分片、已提交操作与检查点的按记录合并及稳定业务版本规则；验证不同子域共享数据模型、相同时间决胜、延迟旧采样不回滚新采样、从头重看能够保存低进度。
- [x] 2.4 实现未提交批次隔离和同事务重试，替换修改缓存后再尝试写入的路径；在每个块写入及清单发布阶段注入失败或实例终止，验证正式查询和统计不暴露半完成批次。
- [x] 2.5 实现变更信号与提交枚举补齐，更新启动、切回和写入前的同步流程；故意丢弃中间信号并关闭写入实例，验证新实例仍能恢复全部已提交记录。
- [x] 2.6 实现分片检查点与按完整提交回收，保留删除标记及原业务版本；验证两个整理实例并发、整理中新增记录、部分块替代和任意阶段中断均不丢记录，并记录冗余操作清理前后数量。
- [x] 2.7 实现暂存块数量/占用报告、停止全部写入后的显式离线清理及挂起事务恢复检查；禁止在线自动过期删除，验证缺块事务不能发布成功、补齐后可重试，离线清理不影响已提交历史。
- [x] 2.8 完成升级初始化及旧版兼容快照的恢复路径；使用合成旧分片验证重复升级不重复导入，停写后生成的回退快照包含最新记录与正确删除结果。

## 3. 来源合并、备份、导入与删除

- [x] 3.1 将本地播放和官方历史同步接入来源策略，区分采样时间、官方观看时间及提交顺序；验证五个百分点边界、保留现有 currentTime、重看回退及并发前置重读用例。
- [x] 3.2 修复备份有效期、新旧比较、删除检查及成功后清理，捕获 localStorage 自身失败；验证 80% 新记录不被 20% 旧备份覆盖、过期备份不恢复、写入失败后仍可重试。
- [x] 3.3 实现整个导入文件的无副作用校验与规范化，两个导入入口共用流程；验证 null、非法 key/字段/日期、规范化重复 key 均拒绝且缓存不变，合法旧格式和已有记录跳过仍正确。
- [x] 3.4 将单条、批量、保留周期删除及撤销接入删除标记和提交身份；验证旧备份不复活记录、删除后新观看可以保存、撤销不覆盖新记录，以及原清理范围和二次确认场景。

## 4. 分 P 索引与查询完整性

- [x] 4.1 实现索引完整性、覆盖信息和 base 加载状态，关联查询等待全部目标分片；验证先读 P1 后仍返回 P1/P2/P3，最新续播目标不受读取顺序影响。
- [x] 4.2 修复新增、删除、导入和恢复后的索引增量维护，禁止局部 Map 覆盖完整索引；验证 80 条旧记录加一条新记录后全部 81 条可查。
- [x] 4.3 实现可靠未命中缓存、远程变更失效及同版本修复合并；验证有效索引下五次未知查询零持久化写入，缺失索引只启动一次实例内重建，远程新增后空结果及时失效。

## 5. 播放会话与续播生命周期

- [x] 5.1 在路由和分 P 切换时立即隔离旧会话写入，绑定 key、标题、媒体及 generation；用可控事件验证 URL 先变和媒体先变均不把旧进度写到新视频。
- [x] 5.2 将暂停、卸载和销毁保存改为沿用会话有效采样与业务版本，正确收集对应标题；验证快速切 P、旧元素复用、延迟回调和新会话低进度记录。
- [x] 5.3 整合同会话媒体重建与首次超时后的可停止发现机制；验证每个活动媒体仅一组事件监听，超时后出现的视频能继续记录，销毁后观察器和轮询停止。
- [x] 5.4 将续播提示、loadedmetadata 跳转和从头播放回调绑定会话身份并清理；验证等待续播时切换视频不会跳转或播放新媒体。

## 6. 标记定向刷新与订阅释放

- [x] 6.1 让数据订阅返回取消函数并合并变更范围，分别处理记录与全局显示设置；验证去抖期间多个 key 都被保留，取消订阅后不再调用回调。
- [x] 6.2 增加完整渲染签名和就地标签更新，按视频/base 关联定向入队；验证关闭进度条、位置/透明度/阈值及多 P 详情变化立即生效，无关卡片节点不重建。
- [x] 6.3 清理路由变化和移除节点的强引用，并使面板具备统一 dispose；验证打开关闭 20 次后订阅数量回到基线，移除的卡片不再进入处理队列。

## 7. 历史查询与统计性能

- [x] 7.1 建立按数据版本缓存的轻量行模型及过滤排序结果，翻页与选择复用结果；在 10000 条数据上验证连续翻页零全量读取/排序且分页内容正确。
- [x] 7.2 对数据准备、过滤和大规模排序使用分段计算及过期任务取消；验证 A 搜索晚于 B 返回时只显示 B，加载中可关闭面板，删除或导入后旧结果不覆盖新状态。
- [x] 7.3 让统计共享行模型并缓存数据版本/阈值/时间范围结果；验证 7/30/90 天、低高阈值边界和空数据的数字与现有口径一致，重复查询复用结果。

## 8. 管理面板基础与视觉体系

- [x] 8.1 在单脚本内建立面板控制器和独立页签职责，保留现有入口委托与默认设置页；通过合成页面打开所有入口，验证不会重复挂载面板且配置草稿与记录状态分离。
- [x] 8.2 按 design.md 的色彩、排版、间距、尺寸和图标规则重写面板及公共组件样式，限定根节点与变量作用域；截图检查深石墨导航、暖白内容、蓝色主操作，并对照确认站点原生控件样式未变化。
- [x] 8.3 实现桌面侧栏、固定页头/操作区与窄屏顶部导航；在 1440×900、1024×768、390×844 下验证关闭和主操作始终可达，面板无整体横向溢出。
- [x] 8.4 实现弹窗焦点栈、Tab 循环、Escape、滚动恢复和减少动态效果支持；通过键盘及 200% 缩放测试验证焦点返回入口、内层关闭不误关外层且无滚动状态残留。

## 9. 设置页完整重构

- [x] 9.1 按播放提示、页面标记、标签样式、诊断维护重排全部现有配置与操作，实现统一开关、位置选择、透明度和阈值控件；逐项比对原功能并检查字段说明与标签关联。
- [x] 9.2 实现接收草稿配置的独立标签预览，覆盖已访问、低/中/高进度与多 P；交互验证预览即时变化且未保存时正式设置、真实卡片与历史存储均不改变。
- [x] 9.3 实现已保存快照、草稿、字段错误、保存中/成功/失败及固定保存区；验证非法阈值阻止保存，切换页签保留草稿，保存成功后实际卡片和悬浮隐藏规则更新。
- [x] 9.4 实现恢复默认进入草稿，以及保存后关闭、放弃、继续编辑的关闭流程；验证保存失败保留输入，普通无修改关闭无需确认，恢复默认未保存时不改变正式配置。

## 10. 历史管理页完整重构

- [x] 10.1 实现搜索主行、筛选排序、数据维护区域及带状态/进度的列表，接入查询结果缓存；验证搜索不失焦，长标题和 key 可读，默认 20 条及现有分页选项可用。
- [x] 10.2 实现选择数量、批量操作条、跨页选择和即时全选/部分选中状态；验证零选中不可删除、逐条选择正确更新表头、删除后失效 key 被清理。
- [x] 10.3 接入导入导出、保留周期清理和错误/加载/空数据/无匹配结果状态；逐项跑既有数据操作场景并截图检查操作层级及窄屏布局。

## 11. 统计页完整重构

- [x] 11.1 重排数字摘要并统一环形图、状态/进度条形图及近期趋势的 SVG 样式和文字值；使用空数据、阈值边界及长范围数据核对显示数值和图形比例。
- [x] 11.2 实现统一范围选择、加载与空状态，以及高进度比例和最近保存时间的准确说明；验证三个范围切换时标题、选中状态与图表同步，窄屏图表滚动不撑宽面板。

## 12. 公共确认与反馈

- [x] 12.1 用统一确认组件承载保留周期清理和设置关闭流程，保留清理周期、截止时间、数量和取消语义；通过键盘和取消用例验证未确认时不写入数据。
- [x] 12.2 统一 Toast、进度、失败与显式撤销按钮，接入真实提交结果和错误路径；验证反馈高于面板遮罩，失败不显示成功，重复提交被阻止，撤销可用键盘触发。
- [x] 12.3 协调管理面板、续播提示和外部入口的层级与必要视觉样式；验证操作后播放监听继续工作，悬浮隐藏规则与面板位置恢复行为符合原功能。

## 13. 综合验收

- [x] 13.1 运行脚本语法检查及完整隔离回归，补齐不同子域、整理与提交交错的故障矩阵；确认全部存储、索引、播放和生命周期场景通过，并记录实际运行命令与结果。
- [x] 13.2 运行本地 Playwright 验证并逐张检查三个尺寸下的三个页签、草稿、错误、空状态、批量选择、确认、进度与撤销截图；修正溢出、遮挡、不可见焦点和对比度问题后再通过。
- [x] 13.3 运行 10000 条记录的性能验证，记录冷启动、搜索、翻页、统计与内部整理的耗时及存储操作次数；修复超过预算的脚本计算段，确认重复翻页和未知视频查询满足零全量重算/零写入要求。
- [x] 13.4 在可访问的真实 Bilibili 页面检查普通视频、分 P、合集、投稿播放列表、首页、搜索、历史和头部弹窗，并复查历史同步与保留周期的原规范；未能验证的页面明确列出，不以合成结果替代。（2026-09-06 用户确认全部验收通过。）
- [x] 13.5 核对最终变更仅涉及目标脚本、其验证工具及本 OpenSpec 变更，确认没有无关脚本、运行时依赖或网站权限变化；记录实现完成状态，保持脚本版本、根 README 与 CHANGELOG 等待用户明确收尾后再更新。
