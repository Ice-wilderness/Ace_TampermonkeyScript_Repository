## 1. 回归基础与状态边界

- [x] 1.1 在 tools/discuz-helper/ 建立标准列表、详情、无图、受限页、动态替换和慢图片夹具及 GM 模拟器；验证能加载目标脚本并统计页面／图片请求与持久化调用。
- [x] 1.2 在单脚本内分离页面适配、数据、预览控制和视图职责，建立逐页／逐图状态及生命周期代次；验证语法检查通过，DOM 文案或 display 不再决定业务状态。
- [x] 1.3 为现有宽度规则、自动第 1 页与默认 5 张、后续分页默认关闭建立基线用例；验证改造前后兼容项一致。

## 2. 足迹存储和数据交换

- [x] 2.1 实现稳定站点 ID 和版本化字段存储，仅新增必要 GM_listValues 授权；验证不同标题共用记录、不同主机和安装路径隔离、HTTP/HTTPS 兼容。
- [x] 2.2 实现旧键发现、可重入迁移、保留备份和归属不明时的选择导入；验证布尔记录、多标题旧键、中途失败重启和显式 false 不被回灌。
- [x] 2.3 实现多标签页独立字段更新、数据代次及页面恢复同步；用交错测试验证不同帖、同帖不同字段不互相覆盖，清空后旧任务不复活数据。
- [x] 2.4 实现记录容量清理与 30 天清理的并发保护；以 2201 条以上夹具验证近期记录、完整字段及清理期间的新更新得到保留。
- [x] 2.5 实现完整导入校验、预览统计和兼容合并；验证 null、数组、危险键、非法时间、未知版本、超限及异站点文件均按规范处理且拒绝时不部分写入。
- [x] 2.6 实现实时导出、新旧格式选择、对象 URL 释放与数据管理即时反馈；验证同面板导入／清理后导出包含最新结果，写入失败不显示成功。

## 3. 阅读状态语义

- [x] 3.1 将真实访问限定为有效详情正文；验证列表抓取、登录页和验证页不新增 visited，有效详情正常记录。
- [x] 3.2 统一主动意图与成功呈现的看图写入；覆盖自动预览、灯箱、展开、加载更多、无图、全过滤、全失败及陈旧回调，验证状态符合规范。
- [x] 3.3 实现单帖恢复未读、清除看图和 5 秒有条件撤销；验证两个字段独立、清除后旧结果不重标、冲突撤销不覆盖新操作。

## 4. 页面与图片预览流程

- [x] 4.1 修正动态／伪静态分页构造与响应地址下的图片规范化去重；用带查询参数的伪静态 URL、相对地址、高清及懒加载属性夹具验证请求页和图片唯一性。
- [x] 4.2 实现有效正文检查和错误分类；验证 HTTP 200 登录／验证内容不被缓存为无图，手动错误可读且可重试，自动错误不打断浏览。
- [x] 4.3 改为逐页保留和呈现、失败页独立重试；模拟第 3 页失败，验证前两页仍可见、重试不重抓成功页、不漏图或重复呈现。
- [x] 4.4 建立所有页面请求共享的手动优先队列和图片独立限流；验证页面并发配置 1–5、图片上限 4、自动间隔和灯箱目标图优先级。
- [x] 4.5 实现页面 15 秒及图片 20 秒超时、有限重试、429 等待和访问限制不重试；通过可控时钟验证队列不会永久占用或无限重试。
- [x] 4.6 实现后台暂停、折叠暂停、销毁取消及旧回调隔离；验证移除／复用后没有 DOM 回写、看图记录、残留轮询或活动观察任务。
- [x] 4.7 实现逐页 TTL、100 帖／2 MiB 缓存边界和当前帖重新获取；验证过期、配额错误、损坏数据及强制更新都能继续预览且不重复图片。

## 5. 页面挂载与动态列表

- [x] 5.1 将标准表格预览迁至独立行，计算正确列跨度并限制已读弱化范围；验证原站标题、排序和链接不受影响，图片及按钮不透明度正常。
- [x] 5.2 对非标准结构实施安全挂载或降级；验证找不到安全容器时不破坏页面，标记及设置仍可使用。
- [x] 5.3 从初始化监听新增、移除、容器替换和复用，过滤脚本自身变化；连续替换夹具验证每帖仅一套控件、旧资源释放、无循环扫描。

## 6. 前端视觉与交互重构

- [x] 6.1 实现统一视觉 token、浅深色及跟随系统主题、限定作用域样式和内联图标；交付浅深色截图，验证原站样式无污染且文字、焦点可辨识。
- [x] 6.2 重构桌面分类导航与窄屏设置布局、固定操作区和危险操作区；验证 390/768/1440px 均无脚本导致的页面级横向溢出，所有现有配置可访问。
- [x] 6.3 实现外观试改、取消恢复和保存即时生效，以及任务配置更新后的失效处理；验证取消不写入、外观保存不刷新、不保留旧任务结果。
- [x] 6.4 重构状态标签、单帖操作和隐藏帖子临时显示入口；验证隐藏模式可恢复未读，标记优先级与操作反馈正确。
- [x] 6.5 重构图片卡片、占位、折叠计数和独立更多按钮；以横图、竖图、长图及失败图检查完整显示和加载前后占位稳定。
- [x] 6.6 重构灯箱的导航、计数、关闭、失败重试和键盘操作；验证方向键、半区点击、Tab 约束、Esc 与关闭后焦点恢复，慢请求切图不会回写旧图。
- [x] 6.7 优化悬浮入口的视觉、拖动与键盘打开，统一对话框可访问性及减少动画行为；验证窗口缩放后入口可见、拖动不误触设置。

## 7. 集成验收与交付

- [x] 7.1 运行本脚本全部核心回归并检查 userscript 所用 API 与声明一致；在 tools/discuz-helper/ 记录执行命令、结果和失败恢复证据。
- [x] 7.2 完成 390/768/1440px × 浅深色的视觉检查，覆盖设置、列表、灯箱、加载、空结果及错误；交付截图和问题修正记录。
- [x] 7.3 用户于 2026-09-10 确认真实环境验收完成；验收期间反馈的图片尺寸、队列等待、按钮、灯箱、看图语义及预览布局问题已修正。真实验收结论来自用户确认，本地夹具证据单独保留。
- [x] 7.4 复核改动范围、旧配置、备份回退和性能边界，汇总新增权限及验证局限；确认未修改其他脚本，未自动更新版本、README 或 CHANGELOG，向用户交付可审阅实现结果。
