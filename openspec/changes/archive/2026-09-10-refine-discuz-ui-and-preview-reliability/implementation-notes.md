> 最终状态（2026-09-10）：用户确认真实环境验收完成，33/33 项任务完成。版本统一更新为 5.0.0，并同步 README、CHANGELOG 与正式规格。以下开发阶段记录按时间保留，其中“待验收”和“未更新版本”描述仅代表当时状态。

# 实施记录

当前脚本和本地验证已完成，真实环境验收由用户执行，任务 7.3 保留未完成。

已实现单文件职责分离、稳定站点与旧记录迁移、字段状态与代次隔离、严格导入与实时导出、请求与图片双队列、部分分页成功保留、任务销毁、统一视觉、设置即时保存及灯箱可访问交互。

用户在真实页面试用中发现图片撑满屏幕和首次自动预览失败。现已将图片卡片限定尺寸并隔离表格固有宽度计算，加入自动请求失败的一次延迟补试，放宽有效正文内的普通图片识别，并避免积分提示误判。

与初稿设计相比，自动重试采用失败后重新排队并等待至少 3 秒，使等待期间不占用页面名额；自动请求总次数仍不超过两次。桌面缩略图上限为 190×142，窄屏高度为 120，避免少量图片铺满整行。

验证证据见 `tools/discuz-helper/验证报告.md`：33 项 Node 回归、14 项浏览器交互检查，以及三种宽度的浅深色截图。没有使用或创建 task_plan.md、findings.md、progress.md；本变更由 OpenSpec 文件持续跟踪。

新增授权只有 GM_listValues。@version 保持 4.7.0，README 和 CHANGELOG 未变。待用户确认真实效果并明确要求收尾后，再统一处理版本与文档。

## 本轮用户反馈修正

- 自动正文任务仅在标题距视口 500px 内执行，当前可见内容优先；图片任务随观察结果更新可见性。滚动与窗口尺寸变化合并到动画帧唤醒队列，保留页面并发配置、图片并发 4、自动请求间隔及 429 退避。
- 图片在开始下载时挂入卡片，支持浏览器渐进呈现；完整加载成功后才校验尺寸、开放灯箱及判定看图记录。超时或取消清除图片源，失败保留重试入口。图片编码及图床响应仍决定实际可见时间，不保证所有格式都可渐进显示。
- 显示排队、读取正文、图片加载进度，消除空白进度条。按钮增加边框与底色，已访问用蓝色、已看图用琥珀色标签；已读透明效果仅应用标题，避免影响按钮和状态可读性。
- 灯箱图片左右半区恢复对应方向的 SVG 光标，并以相同坐标判断点击方向。
- 本地 Node 33 项、浏览器 14 项通过；真实环境仍由用户验收，7.3 保持未完成。

## 并发 3 仍排队的后续修正

用户报告并发已为 3 仍持续排队。发现上一轮正文可见性硬门槛与布局变化缺少唤醒的组合可能搁置任务；截图不能单独证明是否还存在服务器 429 限流。

- 已入队正文只按可见性排序，不再因标题距离退出执行资格；收起的自动任务仍暂停。
- 有前台排队任务时最多每秒复查，覆盖图片展开导致的位置变化及间隔到期边界；队列清空后停止定时器。
- 队列状态分别显示后台暂停、收起暂停、服务器限流倒计时和实际请求占用数/队列位置；状态变化时才更新 DOM。
- 展开已有预览时同时唤醒正文和图片队列；取消任务后重新调度。
- Node 36 项通过，新增并发 3 的占用/释放、无需滚动的恢复与限流提示、远处正文任务继续执行验证。本轮未重新执行浏览器验收；此前 14 项浏览器结果为上一轮记录。真实站点反馈仍待用户验证。

## 看图语义与面板居中调整

按用户最新反馈，收起后再次展开、加载更多和重试缩略图不再触发已看图；只有主动打开灯箱且成功显示图片才记录，保留已有历史数据。已同步 reading-state 规格与设计说明。预览行改为桌面四边 12px、窄屏上下 8px 左右 6px，使面板上下留白一致，保留网站自身 hover 背景。

本轮 Node 36/36、浏览器交互 16/16 通过，覆盖收起再展开不记录、灯箱成功才记录、实际计算样式上下间距一致。OpenSpec 严格校验及语法检查通过，真实论坛验收仍由用户继续。

## 两处无效果按钮修复

临时显示/隐藏之前只影响隐藏样式，现改为独立的页面内可见性覆盖，任意已读样式均可切换；两处入口同步文案、反馈数量，不改历史记录或保存的样式。外观的预览效果原先为空回调，现展开/收起随主题更新的文字样式示例。同步 UI 规格。Node 36 项与浏览器交互 20 项通过；真实验收仍待用户反馈。
